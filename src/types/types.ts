export type MenuItemT = {
  title: string;
  description: string;
  price: string;
};

export type SubCategoryT = {
  category: string;
  subCategory: string;
  title: string;
  description: string;
  price: string;
};
